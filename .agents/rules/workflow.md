# Development Workflow

- Pin exact versions when adding dependencies or devDependencies (no `^` or `~` ranges).
- Verify changes with `pnpm format:write` and `pnpm lint` — both should produce 0 errors.
